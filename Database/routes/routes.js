const express = require('express')
const express = express.Router()
const CandidateTemplate = require('../models/InformationModel')

router.post('/send', (request, response) => {
    const data = new CandidateTemplate({

    })
})